[package]
name = "fstdict_common"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Match this to your exact tauri v2 version dependency string
tauri = { version = "2.0" } 
